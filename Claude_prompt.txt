
---

  Use multiple agents to perform a thorough, line-by-line review of every class under `./src/main/java`, focusing on Javadoc improvements and confirmed bug fixes.

  Divide the review by package or class group. Each agent should inspect related callers, tests, sibling classes, primitive/generated variants, and analogous implementations when needed. The main agent should consolidate findings,
  remove duplicates, and ensure fixes are consistent across related code.

  Code review goals:

  - Identify correctness bugs, edge cases, API contract violations, null-handling issues, resource leaks, concurrency risks, and regression risks.
  - Triage findings by severity and confidence.
  - Fix confirmed bugs only.
  - Preserve existing APIs unless a confirmed bug requires otherwise.
  - Add or update unit tests for every code fix in the existing relevant test classes.
  - Run targeted tests for each changed area.
  - Run the full test suite after all fixes are complete.

  Javadoc review goals:

  - Verify that Javadoc accurately describes the actual behavior.
  - Check `@param`, `@return`, and `@throws` tags against method signatures, parameter names, return types, and thrown exceptions.
  - Flag and fix stale, copy-paste, contradictory, incomplete, or incorrect descriptions.
  - Flag and fix missing, extra, or misnamed `@param` tags.
  - Remove `@return` tags from `void` methods.
  - Add missing `@return` tags for non-void methods where appropriate.
  - Flag and fix undocumented or incorrectly documented exceptions.
  - Verify broken, stale, or incorrect `{@link}` and `@see` references.
  - Improve unclear wording, grammar, typos, incomplete sentences, and descriptions that merely restate the method name.
  - Keep terminology, phrasing, tag ordering, and formatting consistent across the codebase.
  - Pay special attention to sibling and variant classes, such as `CharTuple` vs. `LongTuple`, primitive-type variants, and arity variants. Analogous members should document the same concept with the same wording.

  Fix all confirmed Javadoc and comment issues within scope.

  Final output should include:

  - Summary of code fixes made.
  - Summary of Javadoc/comment fixes made.
  - Tests added or updated.
  - Targeted test commands run and results.
  - Full test suite command and result.
  - Any remaining findings that were not fixed, with severity, confidence, and rationale.


==============================================================================================================================================

-- ==========================================================

Use multiple agents to fix all javadoc issues reported by running: mvn javadoc:javadoc.

Use multiple agents to perform a thorough deep line by line review for all the classes under folder: ./src/main/java for performance improvements.
Fix/implement all performance improvements you have found, and add or run unit tests for the fixes to existing test classes.

-- ==========================================================

Review all public methods in the classes located under foler: ./src/main/java/ for naming convention consistency, and provide suggestions for design and method naming improvements.


==============================================================================================================================================

Use multiple agents to perform a thorough deep line by line review for all the classes under folder: ./src/main/java to find bugs and fix them, and add unit tests for the fixes to existing test classes.

Use multiple agents to perform a thorough deep line by line review for all the classes in packages: com.landawn.abacus.util  under folder: ./src/main/java to find bugs and fix them, and add unit tests for the fixes to existing test classes.


Use multiple agents to perform a thorough deep line by line review for all the classes in package: com.landawn.abacus.util under folder: ./src/main/java to find bugs and fix them, and add unit tests for the fixes to existing test classes.

Use multiple agents to perform a thorough deep line by line review for all the classes under folder: ./src/main/java to find bugs and fix them, and add unit tests for the fixes to existing test classes. Exclude the classes in package: com.landawn.abacus.util and its sub packages.



Are you sure you have identified and fixed all the bugs? Perform a thorough deep review again.

-- ==========================================================

Use multiple agents to perform a thorough deep line by line review for javadocs in all the classes under folder: ./src/main/java to improve/correct existing javadocs

Use multiple agents to perform a thorough deep line by line review for javadocs in all the classes in packages: com.landawn.abacus.util  under folder: ./src/main/java to improve/correct existing javadocs. Don't add new javadocs.


Use multiple agents to perform a thorough deep line by line review for javadocs in all the classes in package com.landawn.abacus.util under folder: ./src/main/java to improve/correct existing javadocs


Use multiple agents to perform a thorough deep line by line review for javadocs in all the classes under folder: ./src/main/java to improve/correct existing javadocs. Exclude the classes in package: com.landawn.abacus.util and its sub packages.


-- ==========================================================

Follow the steps below strictly and execute them in order.

Step 1 - Use multiple agents to review the Javadoc comments for all public methods in the in all classes and interfaces under folder: ./src/main/java/. Ensure that similar methods have consistent and harmonized documentation, including clear and uniform descriptions of method behavior, parameters, and exceptions. For methods annotated with @Override, just review, improve and verify existing javadoc for them. Don't add or generate new javadoc for methods annotated with @Override.

Step 2 - Use multiple agents to generate comprehensive Javadoc for all public methods, including public static methods but skip the methods annotated with @Override, in all classes and interfaces under folder: ./src/main/java/. The Javadoc should be detailed and precise, thoroughly explaining the method’s purpose, behavior, parameters, return value, and exceptions. Where applicable, include a concise usage example (a couple of lines). The example block should always be placed before any @param, @return, or other annotation tags. If Javadoc already exists, review it against the actual implementation and update it for accuracy and clarity. Finally, merge the new or revised Javadoc directly into source file, replacing any existing documentation. DON'T skip any class. For methods annotated with @Override, just review, improve and verify existing javadoc for them. Don't add or generate new javadoc for methods annotated with @Override. Use below template for usage example in Javadoc:
     * <p><b>Usage Examples:</b></p>
     * <pre>{@code
     * Sample codes
     * }</pre>

Step 3 - Use multiple agents to verify every sample/example in all Javadoc across all classes and interfaces under ./src/main/java/ by actually running the implementations, ensuring that all samples/examples pass. If a class import is required during verification, add the import only in the test class, do not add the import to the original Javadoc.

Step 4 - Run mvn javadoc:javadoc to verify all changes are valid


-- ==========================================================


Follow the steps below strictly and execute them in order.

Step 1 - Use multiple agents to review the Javadoc comments for all public methods in the in all classes and interfaces in package: com.landawn.abacus under folder: ./src/main/java/. Ensure that similar methods have consistent and harmonized documentation, including clear and uniform descriptions of method behavior, parameters, and exceptions. For methods annotated with @Override, just review, improve and verify existing javadoc for them. Don't add or generate new javadoc for methods annotated with @Override.

Step 2 - Use multiple agents to generate comprehensive Javadoc for all public methods, including public static methods but skip the methods annotated with @Override, in all classes and interfaces in package: com.landawn.abacus under folder: ./src/main/java/. The Javadoc should be detailed and precise, thoroughly explaining the method’s purpose, behavior, parameters, return value, and exceptions. Where applicable, include a concise usage example (a couple of lines). The example block should always be placed before any @param, @return, or other annotation tags. If Javadoc already exists, review it against the actual implementation and update it for accuracy and clarity. Finally, merge the new or revised Javadoc directly into source file, replacing any existing documentation. DON'T skip any class. For methods annotated with @Override, just review, improve and verify existing javadoc for them. Don't add or generate new javadoc for methods annotated with @Override. Use below template for usage example in Javadoc:
     * <p><b>Usage Examples:</b></p>
     * <pre>{@code
     * Sample codes
     * }</pre>

Step 3 - Use multiple agents to verify every sample/example in all Javadoc across all classes and interfaces in package: com.landawn.abacus under ./src/main/java/ by actually running the implementations, ensuring that all samples/examples pass. If a class import is required during verification, add the import only in the test class, do not add the import to the original Javadoc.

Step 4 - Run mvn javadoc:javadoc to verify all changes are valid


-- ==========================================================

Follow the steps below strictly and execute them in order.

Step 1 - Use multiple agents to verify every sample/example in all Javadoc across all classes and interfaces under ./src/main/java/ by actually running the implementations, ensuring that all samples/examples pass. If a class import is required during verification, add the import only in the test class, do not add the import to the original Javadoc.

Step 2 - Use multiple agents to review Javadoc against the actual implementation and update it for accuracy and clarity. Finally, merge the new or revised Javadoc directly into source file, replacing any existing documentation. DON'T skip any class. For methods annotated with @Override, just review, improve and verify existing javadoc for them. Don't add or generate new javadoc for methods annotated with @Override. Use below template for usage example in Javadoc:
     * <p><b>Usage Examples:</b></p>
     * <pre>{@code
     * Sample codes
     * }</pre>

Step 3 - Run mvn javadoc:javadoc to verify all changes are valid

-- ==========================================================


Read the Eclipse-generated HTML coverage reports located in ./report/coverage/260606/ batch by batch.
Each batch contains 20 HTML files, ordered by highest "Missed Instructions" first (start with the classes that have the most uncovered instructions).
For each class report in the batch:

  - Identify the lines that are not covered by existing tests.
  - Add unit tests that execute those uncovered lines.

The newly added unit tests must follow the requirements below:

  - Use ONLY org.junit.jupiter.api.Assertions:
    assertEquals, assertNotNull, assertTrue, assertThrows, assertDoesNotThrow, etc.

  - Every test method: public void, annotated with @Test, contains at least one
    meaningful assertion.

  - Naming pattern:
      testMethodName()            -- primary / happy-path case
      testMethodName_EdgeCase()   -- named edge case
        Examples: testMethodName_EmptyInput
                  testMethodName_NullKey
                  testMethodName_Overflow

  - Add a one-line comment above each new test group if the intent is not obvious
    from the method name.

  - Use methods from Suppliers and IntFunctions to create Supplier and IntFunction instances for input parameters whenever appropriate.

  - Avoid using size()/length() > 0 or < 0 in assertions. When possible, use assertEquals(expectedSizeOrLength, size()/length()) instead.

  - Before adding a test method, check whether a method with the same name already
    exists in the test class. If so, append a disambiguating suffix to avoid
    duplicate method names.

  - If no test class exists yet, CREATE one in the same package under
    ./src/test/java/, named {SourceClassName}Test.java, extending TestBase.

  - If a public method is abstract, part of a generated API, or genuinely
    untestable in isolation, SKIP it and leave a // TODO: comment in the test
    class explaining why.


After completing each batch, run only the newly added test methods. If any tests fail, fix them — updating the source code if the failure is caused by a bug, or correcting the test itself if the test is wrong.


